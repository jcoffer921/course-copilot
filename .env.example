ANTHROPIC_API_KEY=sk-ant-...

# Google Sign-In (see README "Setup" for how to obtain these from the
# Google Cloud Console OAuth client).
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Everyone can sign in now (UserSettings.access_status is the real access
# gate, enforced separately — see ALLOWED_GOOGLE_EMAILS below). This only
# controls who bootstraps in as already-active vs pending: "allowlist"
# bootstraps ALLOWED_GOOGLE_EMAILS only, "open" bootstraps every new sign-in
# as active.
ONTRACK_ADMISSION_MODE=allowlist
ONTRACK_LOGIN_RATE_LIMIT=10
ONTRACK_LOGIN_RATE_WINDOW_SECONDS=300
ONTRACK_AI_BURST_RATE=8/min
ONTRACK_AI_DAILY_RATE=100/day

# Bootstrap-only now, NOT the access gate: any Google account can sign in
# (subject to ONTRACK_ADMISSION_MODE below) and gets a UserSettings row,
# defaulting to access_status="pending" until approved in Django admin. An
# email on this comma-separated list instead gets that row created as
# already "active", so the developer and any pre-approved accounts don't
# need a manual approval step. Empty/unset means nobody bootstraps active —
# every new sign-in lands on the pending page until approved.
ALLOWED_GOOGLE_EMAILS=you@example.com,teammate@example.com

# Production-only Django security settings.
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=
DJANGO_SECURE_SSL_REDIRECT=true
DJANGO_SECURE_HSTS_SECONDS=0
DJANGO_TRUST_PROXY_SSL_HEADER=false

# Reminder email delivery. The console backend is the safe local default.
# Set the SMTP backend and credentials in production, then schedule
# `python manage.py send_notifications` daily. Running it more often is safe:
# the service enforces at most one email per user per local calendar day.
DJANGO_EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DJANGO_EMAIL_HOST=smtp.example.com
DJANGO_EMAIL_PORT=587
DJANGO_EMAIL_HOST_USER=
DJANGO_EMAIL_HOST_PASSWORD=
DJANGO_EMAIL_USE_TLS=true
DJANGO_DEFAULT_FROM_EMAIL=OnTrack <reminders@example.com>
ONTRACK_SUPPORT_EMAIL=support@example.com
ONTRACK_DKIM_SELECTOR=google
ONTRACK_BASE_URL=http://127.0.0.1:8000
# Register these exact Authorized redirect URIs on the Google OAuth web client:
# http://127.0.0.1:8000/accounts/callback/
# http://127.0.0.1:8000/accounts/calendar/callback/

# Private material upload and processing bounds.
ONTRACK_MAX_UPLOAD_BYTES=10485760
ONTRACK_MATERIAL_PROCESSING_TIMEOUT_SECONDS=90

# Email of the Django user the CLI management commands (extract_syllabus, chunk_notes,
# ask, quiz, mastery, references, reminders, sessions, domains, grades) operate as.
# These commands are dev/debug tools only, not used by real students. Must match a
# user who has already signed in through the web UI at least once.
CLI_OWNER_EMAIL=you@example.com

# Exactly one active account may access /analytics/, its API, and CSV export.
# Defaults to CLI_OWNER_EMAIL when omitted.
ONTRACK_PILOT_OWNER_EMAIL=you@example.com
